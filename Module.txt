import datetime as dt

time = dt.date.today()
print(time )
#-------------------------------------------------------------------------------------------------
#Factorials
import my_module as mod

x = int(input("Please enter the integer you want the facotiral of: "))
          # a = mod.factorial(x)
          # print(a)

mod.factorial(x)

MODULE:


def factorial(x):
  y = 1
  for i in range(x,0,-1):
    y *= i
  #return y
  print("The factorial of {} is {}".format(x,y))